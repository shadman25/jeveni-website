# JEVENI Static Website

Structure:

- `/index.html` - public website
- `/admin/index.html` - admin interface at `/admin/`

Current status:
- Public website is static.
- Admin UI is a visual placeholder.
- Supabase Auth, product CRUD, image upload, and database security will be connected in the next phase.
- Do NOT treat the current admin login as secure authentication.

Cloudflare Pages:
- Deploy this folder as the project root.
- The public site will be `/`.
- The admin page will be `/admin/`.
